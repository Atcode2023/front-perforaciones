// Central place for image base URL so it can be used synchronously across the app
export const IMG_URL = 'https://apiperforaciones.atcode.dev';

// If you need a different value per environment, replace this with an env var
// or set it during boot from server-provided config.
